The copied memorial audio files live here and are used by `src/App.jsx`.

Browsers often block audible autoplay until a visitor interacts with the page. The app attempts to play immediately and also shows a clear play button when the browser blocks it.
